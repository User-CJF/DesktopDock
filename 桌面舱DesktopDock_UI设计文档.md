# 桌面舱 DesktopDock — UI 设计文档

> **版本**：v1.0
> **日期**：2026-08-30
> **文档类型**：UI 设计规范
> **目标读者**：UI/UX 设计师、前端开发工程师
> **配套文档**：《桌面舱 DesktopDock 完整开发文档》

---

## 目录

1. [设计概述](#1-设计概述)
2. [色彩系统](#2-色彩系统)
3. [字体排版规范](#3-字体排版规范)
4. [间距与圆角](#4-间距与圆角)
5. [阴影与层级](#5-阴影与层级)
6. [图标规范](#6-图标规范)
7. [核心组件设计](#7-核心组件设计)
8. [页面布局设计](#8-页面布局设计)
9. [交互动效规范](#9-交互动效规范)
10. [深色模式与主题切换](#10-深色模式与主题切换)
11. [响应式与高DPI适配](#11-响应式与高dpi适配)
12. [空状态与异常状态设计](#12-空状态与异常状态设计)
13. [无障碍设计建议](#13-无障碍设计建议)

---

## 1. 设计概述

### 1.1 设计定位

「桌面舱 DesktopDock」的界面设计目标是营造**类手机 Launcher 的轻量、整洁、高效**体验，弱化传统 Windows 桌面软件的厚重感，让用户在 Windows 平台上获得接近移动端启动器的操作流畅度。

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| **即搜即达** | 搜索是核心交互，任何界面都不应遮挡或拖慢搜索路径 |
| **轻量透明** | 大量使用毛玻璃、柔和阴影、圆角，避免生硬边框 |
| **克制留白** | 页面边距、卡片间距充足，避免图标拥挤 |
| **即时反馈** | 拖拽、点击、悬停均有明确的视觉/动效反馈 |
| **一致性** | 组件在主界面、搜索面板、设置页保持统一的视觉语言 |

### 1.3 目标用户与使用场景

- 软件安装量较大、需要频繁切换工具的重度电脑用户
- 设计师/开发者等效率优先人群
- 核心使用场景：全局热键快速唤起 → 搜索/浏览 → 启动 → 面板消失，全程追求"无感"操作

---

## 2. 色彩系统

### 2.1 品牌色

| 名称 | 色值 | 用途 |
|------|------|------|
| 主色-蓝 | `#4F6BFF` | 主按钮、选中态、链接、焦点框 |
| 主色-紫 | `#8B5CF6` | 渐变、强调元素 |
| 品牌渐变 | `linear-gradient(135deg, #4F6BFF, #8B5CF6)` | Logo、启动页、封面类元素 |

### 2.2 浅色主题色板

| 名称 | 色值 | 应用场景 |
|------|------|----------|
| 背景主色 | `#F5F7FA` | 主窗口背景 |
| 卡片背景 | `#FFFFFF` | 分类卡片、列表项、弹窗 |
| 次级背景 | `#EEF0F5` | 输入框、次级容器背景 |
| 文字主色 | `#1A1A2E` | 标题、正文 |
| 文字次色 | `#6B7280` | 副标题、说明文字 |
| 文字辅助 | `#9CA3AF` | 占位符、禁用文字 |
| 边框 | `#E5E7EB` | 卡片边框、输入框边框 |
| 分割线 | `#F0F0F5` | 列表分割线 |

### 2.3 深色主题色板

| 名称 | 色值 | 应用场景 |
|------|------|----------|
| 背景主色 | `#0F0F1A` | 主窗口背景 |
| 卡片背景 | `#1A1A2E` | 分类卡片、列表项、弹窗 |
| 次级背景 | `#252536` | 输入框、次级容器背景 |
| 文字主色 | `#E8E8F0` | 标题、正文 |
| 文字次色 | `#A0A0B8` | 副标题、说明文字 |
| 文字辅助 | `#6B6B80` | 占位符、禁用文字 |
| 边框 | `#2D2D44` | 卡片边框、输入框边框 |
| 分割线 | `#252536` | 列表分割线 |

### 2.4 状态色

| 状态 | 色值 | 应用场景 |
|------|------|----------|
| 成功 | `#00C9A7` | 操作成功提示、完成态图标 |
| 警告 | `#FFA500` | 二次确认提示、风险操作按钮 |
| 错误 | `#FF4757` | 报错提示、删除类操作 |
| 信息 | `#00B4D8` | 一般提示、社交类分类色 |

### 2.5 分类主题色（预设）

| 分类 | 色值 |
|------|------|
| 办公 | `#4F6BFF` |
| 设计 | `#FF6B6B` |
| 开发 | `#8B5CF6` |
| 娱乐 | `#00C9A7` |
| 社交 | `#00B4D8` |
| 工具 | `#FFA500` |
| 其他 | `#9CA3AF` |

### 2.6 色彩使用规范

- 每个界面的强调色（按钮、选中态）统一使用 `--accent-color`（默认 `#4F6BFF`，可在设置中由用户在预设 8 色或自定义取色器中更换）。
- 分类主题色仅用于分类卡片的图标底色/边框强调，不作为全局强调色。
- 状态色仅用于提示类场景，不用于常规按钮或导航元素，避免与警示信息混淆。

---

## 3. 字体排版规范

| 用途 | 字体 | 字号 | 字重 | 行高建议 |
|------|------|------|------|----------|
| 大标题 | 系统默认 sans-serif | 24px | 600 | 32px |
| 页面标题 | 系统默认 sans-serif | 20px | 600 | 28px |
| 卡片标题 | 系统默认 sans-serif | 16px | 500 | 22px |
| 正文 | 系统默认 sans-serif | 14px | 400 | 20px |
| 辅助文字 | 系统默认 sans-serif | 12px | 400 | 16px |
| 软件名称 | 系统默认 sans-serif | 13px | 400 | 18px |

**字体族优先级**：`'Segoe UI', 'Microsoft YaHei', sans-serif`（保证中英文混排下的字重与字面一致性）

**文本处理规则**：
- 软件名称、分类名称超出容器宽度时单行省略（`text-overflow: ellipsis`），不换行，避免破坏网格布局。
- 搜索结果中，匹配到的字符使用强调色高亮（依据 `matchRanges` 字段渲染），字重加粗但不改变字号。

---

## 4. 间距与圆角

### 4.1 间距体系

| 名称 | 值 | 用途 |
|------|-----|------|
| 页面边距 | 24px | 主窗口/设置页整体内边距 |
| 卡片内边距 | 20px | 分类卡片、弹窗内边距 |
| 元素间距（小） | 8px | 图标与文字间距、标签间距 |
| 元素间距（中） | 12px | 同组控件间距 |
| 元素间距（大） | 16px | 不同功能区块内部间距 |
| 区块间距 | 24px | 常用区、分类区等大区块之间 |

### 4.2 圆角体系

| 名称 | 值 | 用途 |
|------|-----|------|
| 卡片圆角 | 16px | 分类卡片、弹窗 |
| 按钮圆角 | 12px | 常规按钮 |
| 搜索框圆角 | 24px | 全局搜索输入框（胶囊形） |
| 输入框圆角 | 10px | 设置项输入框、表单输入框 |
| 小标签圆角 | 6px | Tag、徽标、快捷键标签 |
| 窗口圆角 | 0–24px（默认 16px） | 主窗口/搜索面板整体圆角，用户可在设置中通过滑块调整 |

---

## 5. 阴影与层级

| 名称 | 值 | 用途 |
|------|-----|------|
| 卡片阴影 | `0 2px 8px rgba(0,0,0,0.06)` | 常规静止状态的分类卡片、列表项 |
| 悬浮阴影 | `0 8px 24px rgba(0,0,0,0.12)` | 卡片 hover、下拉菜单、Tooltip |
| 面板阴影 | `0 16px 48px rgba(0,0,0,0.16)` | 搜索面板、主窗口、弹窗等顶层容器 |

**层级建议（z-index 由低到高）**：主窗口内容 → 悬浮提示/下拉菜单 → 右键菜单 → 确认弹窗/Modal → 搜索面板（独立置顶窗口，始终位于系统最上层）→ 拖拽中的图标（跟随鼠标，临时提升层级并叠加悬浮阴影）。

---

## 6. 图标规范

| 用途 | 尺寸 |
|------|------|
| 软件图标（小） | 32×32px |
| 软件图标（中，默认） | 48×48px |
| 软件图标（大） | 64×64px |
| 功能图标（搜索、设置等） | 20×20px |
| 分类图标 | 28×28px |
| 托盘图标 | 16×16px / 32×32px（随系统 DPI 自动选用） |

**图标设计要点**：
- 软件图标直接使用系统提取的原生图标（`icon.js` 提取缓存），不做二次滤镜处理，保证识别度。
- 分类图标默认使用 Emoji（📄🎨💻🎮💬🔧📁），用户可通过 Emoji 选择器自定义，也支持自定义图片路径。
- 功能图标（搜索、设置、关闭、拖拽手柄等）建议使用线性图标风格（stroke-based），线宽 1.5px，风格统一，避免与实心的软件图标混淆。
- 托盘图标随主题切换在彩色（浅色系统任务栏）与灰度（深色系统任务栏）之间自动适配。

---

## 7. 核心组件设计

### 7.1 顶部栏（主界面）

- 高度：56px，横跨整个主窗口宽度。
- 左侧：Logo + 「桌面舱」文字组合。
- 中间：全局搜索框（点击后可直接跳转到搜索面板，或在主界面内联搜索当前列表）。
- 右侧：设置入口图标（20×20px 功能图标）。
- 背景：跟随主题使用卡片背景色，可选开启毛玻璃效果（`--glass-bg` + `--glass-blur: 20px`）。

### 7.2 常用软件网格

- 布局：4×2 网格，共 8 个位置。
- 单元格内容：软件图标（中，48px）+ 软件名称（13px，单行省略）。
- 排序：固定软件始终置顶（按 `pinnedOrder` 排列），其余按使用评分降序排列；不足 8 个时以「添加软件」虚线占位符填充。
- 交互态：
  - **hover**：图标轻微放大（scale 1.05）+ 悬浮阴影。
  - **拖拽中**：图标半透明（opacity 0.6）跟随鼠标，原位置显示虚线占位框。
  - **右键**：弹出上下文菜单（固定到常用 / 取消固定 / 调整顺序 / 移动到分类等）。

### 7.3 分类卡片

- 卡片尺寸：建议宽高比 1:1 或 4:3，圆角 16px，内边距 20px。
- 卡片内容：分类图标（28px）+ 分类名称（16px，卡片标题字重）+ 软件数量（12px 辅助文字，如「12个」）。
- 卡片主题色：以分类颜色作为图标背景色或卡片顶部细边框强调，不覆盖整个卡片底色，保持整体界面克制统一。
- 交互态：
  - **hover**：悬浮阴影 + 轻微上移（translateY -2px）。
  - **拖拽悬停接收区**：软件图标拖到卡片上方时，卡片边框高亮为分类主题色。
  - **空分类**：卡片内显示「拖拽软件到此处」占位提示文字（辅助文字色）。
- 新建入口：分类区右上角「+ 新建」按钮，样式为次级按钮（描边或浅底色 + 强调色文字）。

### 7.4 搜索面板

- 尺寸：宽 600px，高度自适应内容，最大 500px，超出滚动。
- 结构自上而下：
  1. 搜索输入框（48px 高，24px 圆角，自动聚焦，右侧显示 `Esc` 提示标签）。
  2. 标签栏（应用 / 文件 / 设置，`Tab` 键切换，当前选中标签使用强调色下划线或底色）。
  3. 结果列表（每项：图标 + 名称 + 副标题「分类 · 最近使用」等，最多显示 10 条）。
- 结果项交互：
  - **键盘 ↑↓ 选中**：选中项使用次级背景色高亮，不使用阴影（避免面板内视觉过重）。
  - **鼠标 hover**：与键盘选中态视觉一致，确保键鼠操作反馈统一。
  - **匹配字符高亮**：按 `matchRanges` 用强调色标记命中字符。
- 面板容器使用毛玻璃背景（`--glass-bg`）+ 面板阴影，整体悬浮于系统最上层。

### 7.5 设置面板

- 布局：左侧分类导航（外观 / 行为 / 搜索 / 快捷键 / 数据 / 关于），右侧对应设置项列表。
- 设置项行样式：左侧为设置项名称 + 说明文字（可选），右侧为对应控件（开关 Switch / 单选 Radio / 滑块 Slider / 颜色选择器 / 热键录制框 / 按钮）。
- 开关（Switch）：开启态使用强调色底色，关闭态使用边框色描边。
- 热键录制框：点击后边框变为强调色并显示「请按下组合键...」提示文字，捕获成功后以小标签（6px 圆角）形式回显，如 `Alt` + `Space`。
- 颜色选择器：预设 8 色色块（20×20px 圆形）+ 自定义取色器入口。

### 7.6 文件管理组件

- 最近文件列表：分组标题（今天 / 昨天 / 本周 / 更早，辅助文字色）+ 列表项（系统关联图标 + 文件名 + 修改时间）。
- 收藏文件夹卡片：与分类卡片视觉风格一致（图标 + 名称 + 路径辅助文字），预设入口（桌面、下载、文档、图片、视频）不可删除，样式上可用小锁图标或置灰的删除按钮区分。
- 文件预览：
  - 图片类型：hover 时在结果项旁弹出缩略图（建议 160×160px 内，带悬浮阴影）。
  - 文本类型：hover 时以 Tooltip 形式展示前几行内容。
  - 其他类型：hover 显示文件大小、修改时间、类型的信息条。

### 7.7 右键菜单

- 样式：卡片背景色 + 卡片圆角（可适当收窄为 10-12px）+ 悬浮阴影。
- 菜单项：图标（可选，16px）+ 文字（14px），hover 态使用次级背景色高亮整行。
- 危险操作（如「删除分类」）文字使用错误色 `#FF4757`。

### 7.8 确认弹窗 / Toast

- 确认弹窗（Modal）：卡片背景 + 面板阴影，居中显示，包含标题、说明文字、取消/确认按钮（确认按钮根据操作性质使用强调色或错误色）。
- 一键整理预览弹窗：在确认弹窗基础上增加统计信息列表（如「发现 24 个桌面快捷方式」）和复选框选项（隐藏桌面图标 / 创建还原点）。
- Toast 轻提示：用于「已整理 30 个项目」等操作反馈，短暂停留后自动消失，位置建议在窗口顶部居中或右上角。

---

## 8. 页面布局设计

### 8.1 主界面（900×600px，可调整）

```
┌─────────────────────────────────────────┐
│  [Logo] 桌面舱          [搜索框]  [设置] │  ← 顶部栏 56px
├─────────────────────────────────────────┤
│                                         │
│  常用软件                               │
│  [icon][icon][icon][icon]              │  ← 常用区（4×2 网格）
│  [icon][icon][icon][icon]              │
│                                         │
├─────────────────────────────────────────┤
│  分类文件夹                    [+ 新建]  │
│                                         │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐   │
│  │办公│ │设计│ │开发│ │娱乐│ │工具│   │  ← 分类区
│  │12个│ │ 8个│ │15个│ │ 6个│ │ 9个│   │
│  └────┘ └────┘ └────┘ └────┘ └────┘   │
│                                         │
└─────────────────────────────────────────┘
```

### 8.2 搜索面板（600px 宽 × 自适应，最大 500px 高）

```
┌─────────────────────────────────┐
│  🔍 输入搜索...          [esc]   │  ← 搜索框 48px
├─────────────────────────────────┤
│  [应用] [文件] [设置]            │  ← 标签栏
├─────────────────────────────────┤
│  ┌──┐                           │
│  │图标│ 微信        社交 · 常用  │  ← 结果项（高亮匹配字符）
│  └──┘                           │
│  ┌──┐                           │
│  │图标│ 微信文件传输助手         │
│  └──┘                           │
│  ...                            │
└─────────────────────────────────┘
```

- 屏幕位置：顶部居中，距顶部约 15% 屏幕高度。

### 8.3 分类展开视图

- 触发：点击分类卡片，以面板或路由切换形式展开。
- 布局：顶部为分类名称 + 当前分类内搜索框，主体为软件图标网格（可切换大/中/小图标尺寸），支持拖拽调整分类内软件排序。

### 8.4 设置页布局

- 左右分栏：左侧固定宽度导航栏（约 180px），右侧内容区随窗口宽度自适应，内容区最大宽度建议限制在 640px 左右并居中，避免设置项在宽屏下拉伸过长影响可读性。

### 8.5 文件管理页布局

- 顶部：Tab 或分段控制（最近文件 / 收藏文件夹）。
- 主体：最近文件采用列表布局（分组 + 列表项）；收藏文件夹采用卡片网格布局，风格与分类卡片一致。

---

## 9. 交互动效规范

### 9.1 窗口唤起/隐藏动画

| 窗口 | 入场动画 | 出场动画 |
|------|----------|----------|
| 搜索面板 | 从上方下移 20px + 透明度 0→1，时长 200ms，缓动 `cubic-bezier(0.16, 1, 0.3, 1)` | 上移 20px + 透明度 1→0，时长 150ms |
| 主界面 | 缩放 0.95→1.0 + 透明度 0→1，时长 200ms | 缩放 1.0→0.95 + 透明度 1→0，时长 150ms |

### 9.2 自动隐藏与响应规则

| 触发条件 | 界面表现 |
|----------|----------|
| 按下 `Esc` | 当前窗口按对应出场动画消失 |
| 点击窗口外区域 | 搜索面板自动隐藏（主界面不自动隐藏，保留在原状态） |
| 再次按下唤起热键 | 显示/隐藏状态切换，动效同上 |
| 启动软件后 | 搜索面板按出场动画自动隐藏 |
| 最小化到托盘 | 窗口消失但无出场动效（视为直接关闭） |

### 9.3 常规交互反馈

- 按钮：hover 时轻微加深底色或增加悬浮阴影；点击（active）时轻微缩小（scale 0.97）提供按压反馈。
- 卡片：hover 时上移 2px + 悬浮阴影，过渡时长建议 150ms，缓动 `ease-out`。
- 拖拽：被拖拽元素半透明跟随鼠标，合法放置目标高亮边框，非法放置区域鼠标样式变为「禁止」图标。
- 输入框：聚焦时边框变为强调色，可叠加轻微外发光（box-shadow 使用强调色低透明度）。

### 9.4 全局动效开关

- 设置中「动画效果」开关关闭时，以上所有过渡与位移动画应统一降级为瞬时切换（transition-duration: 0），仅保留必要的状态变化，兼顾低性能设备与用户偏好。

---

## 10. 深色模式与主题切换

### 10.1 主题变量机制

界面所有颜色均通过 CSS 变量定义，切换主题即为 `html` 标签添加/移除 `data-theme='dark'` 属性：

```css
:root {
  --bg-primary: #F5F7FA;
  --bg-secondary: #FFFFFF;
  --bg-tertiary: #EEF0F5;
  --text-primary: #1A1A2E;
  --text-secondary: #6B7280;
  --text-tertiary: #9CA3AF;
  --border-color: #E5E7EB;
  --accent-color: #4F6BFF;
  --shadow-color: rgba(0, 0, 0, 0.08);
  --glass-bg: rgba(255, 255, 255, 0.7);
  --glass-blur: 20px;
}

[data-theme='dark'] {
  --bg-primary: #0F0F1A;
  --bg-secondary: #1A1A2E;
  --bg-tertiary: #252536;
  --text-primary: #E8E8F0;
  --text-secondary: #A0A0B8;
  --text-tertiary: #6B6B80;
  --border-color: #2D2D44;
  --shadow-color: rgba(0, 0, 0, 0.3);
  --glass-bg: rgba(26, 26, 46, 0.7);
}
```

### 10.2 主题模式

- 三种模式：浅色 / 深色 / 跟随系统（默认）。
- 跟随系统模式下监听 `window.matchMedia('(prefers-color-scheme: dark)')`，系统主题变化时界面实时同步，无需重启应用。
- 主色调（`--accent-color`）与主题模式相互独立，用户可在任意主题下自定义强调色（预设 8 色 + 自定义取色器）。

### 10.3 毛玻璃效果

- 默认开启，作用于顶部栏、搜索面板、弹窗等悬浮容器背景（`--glass-bg` + `backdrop-filter: blur(20px)`）。
- 设置中可关闭，关闭后对应容器使用不透明的 `--bg-secondary` 纯色背景，保证低性能设备下的渲染流畅度。

---

## 11. 响应式与高DPI适配

- 主窗口尺寸默认 900×600px，用户可拖拽调整，最小尺寸建议不低于 720×480px，以保证常用区 4 列网格与分类区不换行错位。
- 需适配 ≥200% 缩放的高 DPI 屏幕：所有图标资源建议提供 @2x/@3x 版本或使用矢量图标，避免缩放后模糊。
- 中英文混排环境下，文字容器需预留足够宽度或采用单行省略策略，避免因文字长度差异（中文更紧凑、英文更长）导致布局错位。
- 搜索面板宽度固定 600px，在高 DPI 缩放下仍应保持屏幕居中定位，垂直位置按屏幕高度的 15% 动态计算，而非固定像素值。

---

## 12. 空状态与异常状态设计

| 场景 | 界面表现 |
|------|----------|
| 常用区不足 8 个 | 剩余位置显示虚线边框「添加软件」占位符，点击可跳转到软件列表选择 |
| 空分类 | 卡片内显示「拖拽软件到此处」提示文字（辅助文字色，居中） |
| 搜索无结果 | 面板内显示居中的空状态插图/图标 + 「未找到相关结果」文字 |
| 索引构建中 | 搜索面板输入框下方显示细进度条或加载动效，结果区显示「索引构建中，结果可能不完整」提示 |
| 数据库损坏/异常 | 顶部栏下方显示醒目的警告条（状态色-警告/错误），提示「数据自动备份并重建」等信息，附「查看详情」入口 |
| 热键冲突 | 设置页热键录制框下方显示红色错误提示文字，如「该组合键已被系统占用，请更换」 |
| 文件整理部分失败 | 整理完成 Toast 之外，追加「查看失败项」链接，点击展开失败文件列表及原因 |

---

## 13. 无障碍设计建议

- **对比度**：正文文字与背景色对比度建议不低于 4.5:1，深色主题下尤其注意次级文字色（`#A0A0B8`）与卡片背景（`#1A1A2E`）的可读性。
- **键盘可达性**：搜索面板、设置面板中的所有可交互元素均应支持 `Tab` 顺序遍历与 `Enter`/`Space` 触发，不依赖鼠标 hover 才能发现的功能（如仅 hover 显示的删除按钮）需提供键盘替代路径。
- **焦点态**：所有输入框、按钮的键盘聚焦态需有清晰的强调色描边或外发光，与鼠标 hover 态视觉区分开，避免用户混淆当前操作方式。
- **色彩独立性**：分类主题色、状态色等信息不应作为唯一的信息传达方式，需搭配文字或图标（如错误提示除红色外应有文字说明），照顾色觉障碍用户。
- **动效敏感性**：全局动效开关（见 9.4）也可视为无障碍适配手段，供对动效敏感的用户关闭位移/缩放类动画。

---

*文档结束。本文档基于《桌面舱 DesktopDock 完整开发文档》中的 UI 设计规范及各模块交互描述整理归纳，具体像素级视觉稿建议以设计师在 Figma/Sketch 中输出的高保真稿为准。*
